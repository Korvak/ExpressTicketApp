
function TicketFilterSidebar(props) {

    return (
        <div className={`ta-ticketfilterbar ta-sidebar h-screen bg-gray-500 ${props.extraclasses}`}>
            sidebar
        </div>
    )
}

export {
    TicketFilterSidebar
};